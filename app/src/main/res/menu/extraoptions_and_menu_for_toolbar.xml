<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ignore="ExtraText">
<!--
Note:
here if you write all items as nested items to main 3 dots image then all items will show when we click that 3 dots only(it dont even consider showAsAction attribute)


we need to put items outside of nested items and make show as action as ifRoom or always then we get that items outside

see below to see and understand clearly
-->
    <item android:id="@+id/extraoptions_menu_camera"
        android:title="camera"
        android:icon="@drawable/outline_camera_alt_24"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/extraoptions_menu_search"
        android:title="search"
        android:icon="@drawable/outline_search_24"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom"
        />
    <!-- we can use any view feature using this attribute(actionViewClass)-->
    <item android:icon="@drawable/baseline_menu_book_24"
        android:title="menu book"
        app:showAsAction="always"
        android:id="@+id/extraoptions_menu_book">
        <menu>
            <item android:id="@+id/extraoptions_menu_camera2"
                android:title="camera"
                android:icon="@drawable/outline_camera_alt_24"
                app:showAsAction="always"
                />
            <item android:id="@+id/extraoptions_menu_search2"
                android:title="search"
                android:icon="@drawable/outline_search_24"
                app:showAsAction="always"
                />
            <item android:id="@+id/extraoptions_menu_group"
                android:title="group"
                android:icon="@drawable/baseline_group_24"
                app:showAsAction="never"

                />
            <item android:id="@+id/extraoptions_menu_settings"
                android:title="settings"
                android:icon="@drawable/baseline_settings_24"
                app:showAsAction="never"

                />


        </menu>
    </item>

</menu>